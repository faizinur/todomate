/register => {email, id, password} || 'ok'
/login => {id, password} || buometrics || {email : '', id: '', password : '', avatar : '', follower: [], following: []}
/logout
/setProfile => {username, avatar} || 'ok'

/addTodo => {...todo, location, status : ['CREATED', 'APPROVED', 'ONGOING', 'FINISH', 'CANCELED]} || 'ok'
/updateTodo => {...todo, location, status } || 'ok'
/finishTodo => {id} || 'ok'
/deleteTodo => {id} || 'ok'
/openToWorldTodo => {id} || 'ok'